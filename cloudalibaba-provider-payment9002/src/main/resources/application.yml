server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.119.129:1111 #配置Nacos地址(虚拟机)

management:
  endpoints:
    web:
      exposure:
        include: '*'